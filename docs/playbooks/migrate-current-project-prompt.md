Migrate the current project into the 0000 monorepo following `/home/ubuntu/0000-full/0000/docs/playbooks/import-service-repository.md`. Infer the source repository and destination service name from this checkout. Preserve all history, branches, uncommitted work, issues, skills, and Codex session data using verified backups. Reconcile existing destination content and run appropriate checks. Then merge and push to monorepo main and verify CI. Retain the original checkout and backups. Record the migration and report any remaining manual step for keeping the dedicated Codex Desktop project and its existing sessions.
