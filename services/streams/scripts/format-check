#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"

if [ "$#" -eq 0 ]; then
    set -- .
fi

exec bunx --yes --package "@biomejs/biome@2.5.13" biome format --no-errors-on-unmatched "$@"
