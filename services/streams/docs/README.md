# Streams documentation

Future event and stream design belongs here after the database boundary and
runtime decisions are reviewed. The imported Worker implementation is in
`src/`; its current Durable Object SQLite storage has not been verified against
the canonical `0000-database` dependency.
