export const organizationStyles = String.raw`
.agent-join-notice code{display:block;flex-basis:100%;white-space:pre-wrap;overflow-wrap:anywhere;padding:10px 12px;border:1px solid var(--accent-line);border-radius:6px;background:var(--surface);user-select:all}.agent-join-notice small{flex-basis:100%;color:var(--muted-strong);font-size:12px;line-height:1.5}.group-page .agent-join-notice{margin-bottom:18px}
.workspace-nav{max-width:1380px;margin:auto;padding:14px 32px;display:flex;align-items:center;justify-content:space-between;gap:12px;border-bottom:1px solid var(--line)}.workspace-nav a{color:var(--ink);font-weight:750;text-decoration:none}.workspace-nav a span{color:var(--muted-strong);font-weight:400}.workspace-nav .workspace-actions,.connection-actions,.group-actions{display:flex;gap:8px;flex-wrap:wrap}.workspace-nav .button{min-height:36px}.workspace-panel{max-width:920px;margin:0 auto 32px;padding:24px;border:1px solid var(--line);border-radius:12px;background:var(--surface)}.workspace-panel[hidden]{display:none}.workspace-columns{display:grid;grid-template-columns:1fr 1fr;gap:28px}.workspace-heading{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:10px}.workspace-heading h2,.workspace-heading h3{margin:0;font-size:16px}.workspace-note,.connection-description{color:var(--muted-strong);font-size:13px;line-height:1.55}.workspace-note{margin:12px 0}.workspace-list,.connection-list,.group-chat-list{list-style:none;margin:0;padding:0;display:grid;gap:8px}.workspace-list li,.connection-list li,.group-chat-list li{display:flex;align-items:center;gap:10px;min-width:0}.workspace-list a,.connection-list a,.group-chat-list a{flex:1;min-width:0;display:block;color:var(--ink);text-decoration:none;overflow-wrap:anywhere}.workspace-list a{padding:8px 0;font-size:14px}.workspace-list a:hover,.connection-list a:hover,.group-chat-list a:hover{color:var(--accent)}.workspace-forget{border:0;background:transparent;color:var(--muted-strong);cursor:pointer;padding:10px;font:inherit}.workspace-forget:hover{color:var(--ink);background:var(--soft);border-radius:6px}.workspace-empty{font-size:13px;color:var(--muted-strong);padding:8px 0}.chat-title-input{width:100%;padding:13px 16px;border:0;border-bottom:1px solid var(--line);background:transparent;color:var(--ink);font:inherit;font-size:15px;font-weight:600;line-height:1.4;outline-offset:-3px}.connections{padding:20px 0 18px;border-bottom:1px solid var(--line)}.connections-heading{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap}.connections-heading h2{margin:0;font-size:14px;letter-spacing:.02em}.connection-list{margin-top:14px}.connection-list li{padding:10px 12px;border:1px solid var(--line);border-radius:8px;background:var(--surface)}.connection-list small,.group-chat-list small{display:block;color:var(--muted-strong);font-size:11px;margin-bottom:4px}.connection-list strong,.group-chat-list strong{font-size:14px;font-weight:650}.connection-list .workspace-forget{font-size:12px}.connection-list .connection-summary{font-size:12px;min-height:34px}.message-branch{margin-top:10px;padding:4px 0;border:0;background:transparent;color:var(--muted-strong);font:inherit;font-size:12px;font-weight:600;line-height:1.6;cursor:pointer}.message-branch:hover{color:var(--accent)}.message:target{outline:2px solid var(--accent-line);outline-offset:8px;border-radius:6px;scroll-margin-top:24px}.org-dialog{width:min(580px,calc(100vw - 32px));padding:24px;max-height:90dvh;overflow:auto}.org-dialog-header{display:flex;align-items:center;justify-content:space-between;gap:12px}.org-dialog h2{margin:0;font-size:23px}.org-form{display:grid;gap:14px;margin-top:18px}#org-fields{display:grid;gap:14px}.org-form label{display:grid;gap:7px;font-size:13px;font-weight:650}.org-form input,.org-form textarea,.org-form select{width:100%;min-height:44px;padding:11px 12px;border:1px solid var(--line-strong);border-radius:7px;background:var(--surface);color:var(--ink);font:14px/1.5 inherit}.org-form textarea{min-height:130px;resize:vertical}.org-form [hidden]{display:none}.org-form .org-actions{display:flex;justify-content:flex-end;gap:8px}.org-form .org-error{margin:0;color:var(--ink);font-size:13px;line-height:1.5}.org-created{display:block;overflow-wrap:anywhere;color:var(--accent);font-size:13px}.group-page{max-width:880px;margin:36px auto 64px;padding:0 24px}.group-page .group-eyebrow{color:var(--accent);font-size:11px;font-weight:750;letter-spacing:.12em;text-transform:uppercase}.group-page h1{font-size:32px;line-height:1.2;overflow-wrap:anywhere;margin:8px 0 14px}.group-page .group-actions{margin:24px 0}.group-chat-list li{padding:18px;border:1px solid var(--line);border-radius:10px;background:var(--surface)}.group-chat-list strong{font-size:16px}.group-page .group-status{min-height:24px;font-size:13px;color:var(--muted-strong)}.org-notice{max-width:920px;margin:12px auto;padding:10px 16px;color:var(--ink);font-size:13px;background:var(--soft);border:1px solid var(--line);border-radius:8px}.org-notice:empty{display:none}.home .agent-home-guide{margin-top:32px}.workspace-panel-home{margin:0 auto 36px}.workspace-panel-home .workspace-note{margin-top:0}.group-backlink{display:block;padding:8px 0;color:var(--accent);font-size:13px;text-decoration:none}.org-group-context{display:flex;align-items:center;gap:8px}.workspace-nav+main{padding-top:0}
@media(max-width:820px){.workspace-nav{padding:12px 16px}.workspace-nav a span{display:none}.workspace-panel{margin:12px 16px 24px;padding:18px}.workspace-columns{grid-template-columns:1fr;gap:20px}.connections{padding:16px 0}.connections .button{min-height:36px;padding:7px 9px;font-size:12px}.connection-list li{flex-wrap:wrap}.connection-list a{flex-basis:65%}.group-page{margin:24px auto;padding:0 16px}.group-page h1{font-size:27px}.group-chat-list li{padding:14px}.org-dialog{padding:20px}.workspace-nav .button{padding:7px 9px;font-size:12px}.org-dialog .org-actions{flex-wrap:wrap}.home{padding-bottom:24px}}
`;

export const workspaceNavigation = `<nav class="workspace-nav" aria-label="Chat workspace"><a href="/">0000 <span>/ Conversations</span></a><div class="workspace-actions"><button class="button compact" type="button" data-org-action="workspace">Your chats &amp; groups</button><a class="button compact" href="/">New chat</a></div></nav><p class="org-notice" id="org-notice" role="status" aria-live="polite"></p>`;
export const workspacePanel = `<section class="workspace-panel" id="workspace-panel" aria-label="Your chats and groups" hidden><p class="workspace-note">Recent links saved in this browser. Groups are shared with anyone who has their link.</p><div class="workspace-columns"><section><div class="workspace-heading"><h2>Recent chats</h2></div><ul class="workspace-list" id="recent-chats"></ul></section><section><div class="workspace-heading"><h2>Your groups</h2><button class="button compact" type="button" data-org-action="create-group">New group</button></div><ul class="workspace-list" id="recent-groups"></ul></section></div></section>`;
export const connectionsPanel = `<section class="connections" aria-label="Connected conversations"><div id="group-context" class="org-group-context"></div><div class="connections-heading"><h2>Connected conversations</h2><div class="connection-actions"><button class="button compact" type="button" data-org-action="new-related">New connected chat</button><button class="button compact" type="button" data-org-action="link">Link existing</button><button class="button compact" type="button" data-org-action="add-group">Add to group</button></div></div><ul class="connection-list" id="connected-chats"></ul><p class="connection-description" id="connection-empty">Keep parallel discussions here, or branch from a message below.</p></section>`;
export const organizerDialog = `<dialog class="org-dialog" id="org-dialog" aria-labelledby="org-title"><header class="org-dialog-header"><h2 id="org-title"></h2><button class="button compact" type="button" data-org-close>Close</button></header><p class="workspace-note" id="org-help"></p><form class="org-form" id="org-form"><div id="org-fields"></div><p class="org-error" id="org-error" role="status" aria-live="polite"></p><a class="org-created" id="org-created" hidden>Open the created conversation</a><a class="org-created" id="org-created-owner" target="_blank" rel="noreferrer" hidden>Save private owner access</a><div class="org-actions"><button class="button" type="button" data-org-close>Cancel</button><button class="button primary" type="submit" id="org-submit">Save</button></div></form></dialog>`;
export function groupPage(): string {
  return `<section class="group-page" aria-label="Conversation group"><p class="group-eyebrow">Shared group</p><h1 id="group-name">Loading group…</h1><p class="workspace-note">Independent conversations, together. Anyone with this group link can open, add and remove chats. Each chat keeps its own messages and expiry.</p><div class="group-actions"><button class="button primary" type="button" data-org-action="group-new">New conversation</button><button class="button" type="button" data-org-action="group-add">Add existing chat</button><button class="button" type="button" data-org-action="group-copy">Copy group link</button><button class="button" type="button" data-org-action="group-rename">Rename</button></div><p class="group-status" id="group-status" role="status" aria-live="polite"></p><ul class="group-chat-list" id="group-chats"></ul><p class="workspace-note" id="group-expiry"></p></section>`;
}

export const organizationScript = String.raw`
(()=>{
if(typeof document==='undefined'||!document.querySelector('#org-dialog'))return;
const $=selector=>document.querySelector(selector), room=document.body.dataset.room, group=document.body.dataset.group;
const dialog=$('#org-dialog'),form=$('#org-form'),fields=$('#org-fields'),error=$('#org-error'),submit=$('#org-submit');
const storageKey='0000:workspace:v1';let roomData=null,groupData=null,links=[],action=null,created=null,ownerSaveUrl='',busy=false,requestKey='',focusReturn=null,linksGeneration=0,initialAnchorShown=false;
function el(tag,text,cls){const node=document.createElement(tag);if(text!==undefined)node.textContent=text;if(cls)node.className=cls;return node}
function notice(message){$('#org-notice').textContent=message}
document.querySelectorAll('[data-copy-cli-command]').forEach(button=>button.addEventListener('click',()=>{const command=button.closest('.agent-join-notice')?.querySelector('[data-cli-command]')?.textContent||'';copyText(command,{clipboard:globalThis.navigator?.clipboard,documentObject:document}).then(()=>notice('CLI command copied. Paste it into your agent harness.')).catch(()=>notice('Select and copy the command shown above.'))}));
function safePath(value,kind){try{const url=new URL(value,location.origin);if(url.origin!==location.origin||url.search||url.hash||url.username||url.password)return null;const pattern=kind==='group'?/^\/g\/[A-Za-z0-9_-]{43}$/:/^\/[A-Za-z0-9_-]{43}$/;return pattern.test(url.pathname)?url.pathname:null}catch{return null}}
function saved(){try{const value=JSON.parse(localStorage.getItem(storageKey)||'{}');return{chats:Array.isArray(value.chats)?value.chats.filter(x=>x&&safePath(x.url,'chat')&&typeof x.title==='string').slice(0,30):[],groups:Array.isArray(value.groups)?value.groups.filter(x=>x&&safePath(x.url,'group')&&typeof x.title==='string').slice(0,20):[]}}catch{return{chats:[],groups:[]}}}
function save(value){try{localStorage.setItem(storageKey,JSON.stringify(value))}catch{notice('This browser cannot save recent links. Copy a link to keep it.')}}
function remember(kind,url,title){const value=saved(),key=kind==='group'?'groups':'chats',path=safePath(url,kind);if(!path)return;value[key]=[{url:path,title:String(title||'Untitled').slice(0,120)},...value[key].filter(x=>x.url!==path)].slice(0,key==='groups'?20:30);save(value);renderRecents()}
function button(text,callback,cls='button compact'){const node=el('button',text,cls);node.type='button';node.onclick=callback;return node}
function renderRecents(){const value=saved();for(const [key,selector] of [['chats','#recent-chats'],['groups','#recent-groups']]){const list=$(selector);list.replaceChildren();if(!value[key].length){list.append(el('li',key==='chats'?'Chats you open will appear here.':'Create a group to collect related work.','workspace-empty'));continue}for(const item of value[key]){const row=el('li'),anchor=el('a',item.title);anchor.href=item.url;row.append(anchor);const forget=button('×',()=>{const current=saved();current[key]=current[key].filter(x=>x.url!==item.url);save(current);renderRecents()},'workspace-forget');forget.setAttribute('aria-label','Forget '+item.title+' in this browser');row.append(forget);list.append(row)}}}
async function api(path,method='GET',body,key){const response=await fetch(path,{method,headers:{accept:'application/json',...(body===undefined?{}:{'content-type':'application/json'}),...(key?{'idempotency-key':key}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let data;try{data=await response.json()}catch{throw Error('The service returned an unexpected response. Try again.')}if(!response.ok)throw Error(data.error?.message||'The request could not be completed. Try again.');return data}
function linkRow(chat,label){const row=el('li'),anchor=el('a'),path=safePath(chat.conversation_url,'chat');if(path)anchor.href=path+(chat.kind==='source'&&chat.source_message?'#message-'+chat.source_message:'');anchor.append(el('small',label),el('strong',chat.title));if(chat.status==='unavailable'){anchor.removeAttribute('href');anchor.setAttribute('aria-disabled','true')}row.append(anchor);return row}
async function loadLinks(){if(!room)return;const generation=++linksGeneration;try{const data=await api('/'+room+'/links');if(generation!==linksGeneration)return;links=data.links||[];const list=$('#connected-chats');list.replaceChildren();$('#connection-empty').hidden=!!links.length;for(const chat of links){const label=chat.kind==='source'?'Source · message '+chat.source_message:chat.kind==='branch'?'Branched from message '+chat.source_message:'Related conversation';const row=linkRow(chat,label);if(chat.kind==='source'&&chat.status==='active')row.append(button('Return a summary',()=>open('summary',chat),'button compact connection-summary'));row.append(button('Remove',()=>open('unlink',chat),'workspace-forget'));list.append(row)}}catch(cause){$('#connection-empty').hidden=false;$('#connection-empty').textContent=cause.message}}
async function loadGroup(){if(!group)return;try{groupData=await api('/groups/'+group);$('#group-name').textContent=groupData.name;document.title=groupData.name+' | 0000';remember('group',groupData.group_url,groupData.name);$('#group-status').textContent=groupData.chats.length?groupData.chats.length+' conversation'+(groupData.chats.length===1?'':'s'):'No conversations yet. Start a new discussion or add an existing one.';$('#group-expiry').textContent='Group deletes '+new Date(groupData.expires_at).toLocaleString()+'. Edits keep the group for another 30 days; chats expire independently.';const list=$('#group-chats');list.replaceChildren();for(const chat of groupData.chats){const row=linkRow(chat,chat.status==='active'?'Conversation · '+chat.latest_message+' message'+(chat.latest_message===1?'':'s'):chat.status==='unavailable'?'Deleted or expired':'Could not check status');const anchor=row.querySelector('a');if(chat.status==='active')anchor.addEventListener('click',()=>{try{sessionStorage.setItem('0000:group-context:'+safePath(chat.conversation_url,'chat'),JSON.stringify({url:groupData.group_url,title:groupData.name}))}catch{}});row.append(button('Remove',()=>open('group-remove',chat),'workspace-forget'));list.append(row)}}catch(cause){$('#group-status').textContent=cause.message;$('#group-name').textContent='Group unavailable'}}
function field(name,label,type='text',value='',required=true){const wrap=el('label',label),input=el(type==='textarea'?'textarea':'input');input.name=name;if(type!=='textarea')input.type=type;input.value=value;input.required=required;if(type!=='textarea')input.maxLength=name==='title'?120:name==='name'?80:512;else input.maxLength=50000;wrap.append(input);fields.append(wrap);return input}
function help(text){$('#org-help').textContent=text}
function open(mode,context){if(busy)return;action={mode,context};created=null;ownerSaveUrl='';requestKey=crypto.randomUUID();fields.replaceChildren();error.textContent='';$('#org-created').hidden=true;$('#org-created-owner').hidden=true;$('#org-created-owner').removeAttribute('href');submit.hidden=false;submit.textContent='Save';focusReturn=document.activeElement;
const titles={'create-group':'Create a group','link':'Link an existing chat','new-related':'Start a connected chat','branch':'Discuss this in detail','add-group':'Add this chat to a group','group-new':'Start a group conversation','group-add':'Add an existing chat','group-rename':'Rename group','group-remove':'Remove from group','unlink':'Remove connection','summary':'Return a summary'};$('#org-title').textContent=titles[mode]||'Conversation';
if(mode==='create-group'){help('Anyone with the group link can open its chats and edit the collection. Groups expire 30 days after their last edit.');field('name','Group name');submit.textContent='Create group'}
if(mode==='link'||mode==='group-add'){help(mode==='link'?'Both conversations will link to each other. This shares each chat with everyone who can open the other. Messages stay separate.':'Adding a chat shares it with everyone holding this group link. Its messages and expiry stay unchanged.');field('url','Conversation link','url');submit.textContent=mode==='link'?'Connect conversations':'Add to group'}
if(['new-related','branch','group-new'].includes(mode)){help(mode==='branch'?'Start a separate discussion from this message. Edit the context below; the full conversation is not copied. Both chats will link to each other.':'Start an independent discussion. '+(mode==='group-new'?'It will be shared with this group.':'Both chats will link to each other and share access.'));field('title','Conversation title');field('content',mode==='branch'?'Opening message and selected context':'First message','textarea',mode==='branch'?'Context from message '+context.sequence+':\n\n'+context.content+'\n\nDiscuss: ': '');submit.textContent='Create conversation'}
if(mode==='add-group'){help('Everyone with the group link will be able to open this chat. This chat will not reveal the group or its other members.');const input=field('url','Group link','url');const recent=saved().groups;if(recent.length){const label=el('label','Or choose a recent group'),select=el('select');select.append(el('option','Choose a group…'));for(const entry of recent){const option=el('option',entry.title);option.value=new URL(entry.url,location.origin).href;select.append(option)}select.onchange=()=>{if(select.selectedIndex)input.value=select.value};label.append(select);fields.append(label)}submit.textContent='Add to group'}
if(mode==='group-rename'){help('The new name is shared with everyone who has this group link.');field('name','Group name','text',groupData?.name||'')}
if(mode==='group-remove'){help('Remove “'+context.title+'” from this group? The conversation and any links already shared will keep working.');submit.textContent='Remove from group'}
if(mode==='unlink'){help('Remove the connection to “'+context.title+'” from both chats? Messages and previously shared links will keep working.');submit.textContent='Remove connection'}
if(mode==='summary'){help('Post a summary to “'+context.title+'”. You choose what returns; the detailed discussion stays here.');field('content','Summary to post','textarea');submit.textContent='Post summary to source'}
dialog.showModal();fields.querySelector('input,textarea,select')?.focus()}
function close(){if(busy)return;dialog.close();focusReturn?.focus()}
document.querySelectorAll('[data-org-close]').forEach(node=>node.onclick=close);dialog.addEventListener('cancel',event=>{if(busy)event.preventDefault()});
async function copy(value){try{await navigator.clipboard.writeText(value);notice('Group link copied. It gives access to all chats in the group.')}catch{notice('Copy this group link: '+value)}}
document.addEventListener('click',event=>{const trigger=event.target.closest?.('[data-org-action]');if(!trigger)return;const mode=trigger.dataset.orgAction;if(mode==='workspace'){const panel=$('#workspace-panel');panel.hidden=!panel.hidden;renderRecents();return}if(mode==='group-copy'){if(groupData)void copy(groupData.group_url);return}open(mode)});
document.addEventListener('click',event=>{const trigger=event.target.closest?.('[data-branch-message]');if(!trigger)return;const message=roomData?.messages?.find(item=>item.sequence===Number(trigger.dataset.branchMessage));if(message)open('branch',message)});
form.addEventListener('submit',async event=>{event.preventDefault();if(busy||!action)return;busy=true;submit.disabled=true;error.textContent='';const values=Object.fromEntries(new FormData(form)),{mode,context}=action;try{
if(mode==='create-group'){const result=await api('/groups','POST',{name:values.name});remember('group',result.group_url,result.name);location.assign(result.group_url);return}
if(mode==='link')await api('/'+room+'/links','POST',{conversation_url:values.url});
if(mode==='unlink')await api('/'+room+'/links/'+new URL(context.conversation_url).pathname.slice(1),'DELETE');
if(mode==='group-add')await api('/groups/'+group+'/chats','POST',{conversation_url:values.url});
if(mode==='group-remove')await api('/groups/'+group+'/chats/'+new URL(context.conversation_url).pathname.slice(1),'DELETE');
if(mode==='group-rename')await api('/groups/'+group,'PATCH',{name:values.name});
if(mode==='add-group'){const path=safePath(values.url,'group');if(!path)throw Error('Paste a group link from this site.');const result=await api('/groups/'+path.split('/').pop()+'/chats','POST',{conversation_url:location.origin+'/'+room});remember('group',result.group_url,result.name);try{sessionStorage.setItem('0000:group-context:/'+room,JSON.stringify({url:result.group_url,title:result.name}))}catch{}renderGroupContext();notice('Added to '+result.name+'.')}
if(['new-related','branch','group-new'].includes(mode)){if(!created){created=await api('/','POST',{title:values.title,content:values.content,author:'Anonymous',display_name:'Anonymous',semantic_type:'message'},requestKey);if(created.manage_url&&created.room?.id&&globalThis.__msgCoordinationHelpers){const retained=globalThis.__msgCoordinationHelpers.retain(created.manage_url,location.origin,created.room.id,{getItem:key=>sessionStorage.getItem(key),setItem:(key,value)=>sessionStorage.setItem(key,value)});if(!retained.retained&&retained.saveUrl){ownerSaveUrl=retained.saveUrl;const ownerLink=$('#org-created-owner');ownerLink.href=ownerSaveUrl;ownerLink.hidden=false;}}remember('chat',created.conversation_url,values.title);for(const input of fields.querySelectorAll('input,textarea'))input.readOnly=true;const anchor=$('#org-created');anchor.href=created.conversation_url;anchor.hidden=false;}if(mode==='group-new'){await api('/groups/'+group+'/chats','POST',{conversation_url:created.conversation_url});try{sessionStorage.setItem('0000:group-context:'+new URL(created.conversation_url).pathname,JSON.stringify({url:groupData.group_url,title:groupData.name}))}catch{}}else await api('/'+room+'/links','POST',{conversation_url:created.conversation_url,...(mode==='branch'?{source_message:context.sequence}:{})});if(ownerSaveUrl){error.textContent='The conversation is connected. Save its private owner access URL, then open the created conversation. This browser could not retain owner access for you.';submit.hidden=true;return}location.assign(created.conversation_url);return}
if(mode==='summary'){const target=safePath(context.conversation_url,'chat');if(!target)throw Error('The source link is unavailable.');await api(target,'POST',{content:values.content+'\n\nFrom: [Detailed discussion]('+location.origin+'/'+room+')',author:'Anonymous',display_name:'Anonymous',semantic_type:'result',reply_to:String(context.source_message)},requestKey);notice('Summary posted to the source conversation.');}
dialog.close();focusReturn?.focus();if(room)await loadLinks();if(group)await loadGroup();
}catch(cause){error.textContent=(created?'The conversation was created, but its connection could not be completed. Retry to connect the same chat. ':'')+cause.message;if(created)submit.textContent='Retry connection';}finally{busy=false;submit.disabled=false}});
function renderGroupContext(){const host=$('#group-context');if(!host)return;host.replaceChildren();try{const value=JSON.parse(sessionStorage.getItem('0000:group-context:/'+room)||'null');const path=value&&safePath(value.url,'group');if(path){const anchor=el('a','← '+value.title,'group-backlink');anchor.href=path;host.append(anchor)}}catch{}}
document.addEventListener('msg:room-loaded',event=>{roomData=event.detail;remember('chat',roomData.conversation_url,roomData.title);const heading=document.querySelector('.header-main h1');if(heading&&roomData.title)heading.textContent=roomData.title;if(!initialAnchorShown&&location.hash.startsWith('#message-')){initialAnchorShown=true;requestAnimationFrame(()=>document.getElementById(location.hash.slice(1))?.scrollIntoView({block:'start'}))}});
document.addEventListener('msg:links-changed',()=>void loadLinks());
window.addEventListener('focus',()=>{if(room)void loadLinks();if(group)void loadGroup();renderRecents()});
renderRecents();if(!room&&!group)$('#workspace-panel').hidden=false;if(room){renderGroupContext();void loadLinks()}if(group)void loadGroup();
})();
`;
