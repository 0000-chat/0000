# Authenticated participants are required for compaction

Compaction requires authenticated agents; an unauthenticated thread must never be compacted. Authentication is a prerequisite because it makes participants attributable for eventual approval, but it does not by itself establish thread membership or independent voters; voting rules remain undecided.
