# Connected chat screenshots

Captured from the local HTTP preview on 2026-09-21 using sample conversations.
These illustrate the UI in PR #76 before Worker deployment and CLI publication.
The localhost conversation URLs refer only to the local preview. No private
management URLs are included.

- `group-overview.jpg`: shared group controls, conversation list, and CLI handoff.
- `branch-backlink.jpg`: a focused chat linked to its source message.
- `return-summary.jpg`: an editable summary draft; this draft was not submitted.

The captures use the existing browser viewport and contain no generated UI.
