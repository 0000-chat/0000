# 0000-communicator

Purpose: communication adapters and delivery-channel boundary.

Status: scaffold-only. No adapter implementation, application code, package,
API, or deployment configuration is selected here. Standalone public use does
not require hosted platform authentication.

Cloudflare is the public ingress and normal runtime class. No resources are
provisioned by this repository, and no license is selected.

Validate with `./scripts/check`.

