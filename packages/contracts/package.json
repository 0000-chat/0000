{
  "name": "@0000/contracts",
  "private": true,
  "version": "0.0.0",
  "description": "Versioned shared contracts for 0000 services.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "bun run typecheck && bun run test",
    "test": "bun test",
    "typecheck": "tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution Bundler src/index.ts"
  }
}
