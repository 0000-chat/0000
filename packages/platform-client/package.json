{
  "name": "@0000/platform-client",
  "private": true,
  "version": "0.0.0",
  "description": "Portable HTTP verification client for the 0000 Platform authority.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@0000/contracts": "workspace:*"
  },
  "scripts": {
    "check": "bun run typecheck && bun run test",
    "test": "bun test",
    "typecheck": "tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022,DOM src/index.ts"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
